<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Tickets</title>
</head>
<body>
    <h2>Admin: Delete Tickets</h2>

    {{#tickets}}
        <div>
            <h3>{{name}}</h3>
            <p>Price: ${{price}}</p>
            <p>Available: {{quantity_available}}</p>

            <form action="/tickets/delete/{{id}}" method="POST">
                <button type="submit" onclick="return confirm('Are you sure you want to delete this ticket?');">Delete Ticket</button>
            </form>

            <hr>
        </div>
    {{/tickets}}

    {{^tickets}}
        <p>No tickets available to delete.</p>
    {{/tickets}}

    <a href="/users/dashboard">
        <button>Back to Dashboard</button>
    </a>
</body>
</html>
