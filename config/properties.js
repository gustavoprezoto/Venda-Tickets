const PROPERTIES = {
    DATABASE: {
        USER: "root",
        PASSWORD: "Skeleton3(Derail(Mantis(Dragonfly(Obedient",
        NAME: "venda_tickets"
    },

    APPLICATION: {
        PORT: 4000,
    }
}

module.exports = PROPERTIES